# Base URL of the deployed backend API on Render (e.g.
# https://your-app.onrender.com/api). Never put secrets in this file —
# anything here is bundled into public JavaScript.
VITE_API_BASE_URL=http://localhost:5000/api
